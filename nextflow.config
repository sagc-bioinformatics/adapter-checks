params {
    max_memory   = 375.GB
    max_cpus     = 32
    max_time     = 14.d
}

conda.enabled    = true
process {
    conda = "${params.conda_env}"
    executor = 'slurm'
    maxRetries      = 0
}
executor {
    queueSize       = 0
}